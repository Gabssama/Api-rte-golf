<html>
    <head>
        <meta charset="UTF-8">
        <title>Generate PDF File</title>
    </head>
    <body>
        <h4>{{ headline }}</h4>
        <ul>
            {% for product in products %}
                <li>Product : {{ product.name }} | ${{ product.price}}</li>
            {% endfor %}
        </ul>
    </body>
</html>